name: fictional-ci
on: [push]
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Fixture only; the resume scanner must never execute this workflow."

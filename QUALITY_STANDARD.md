# Skill Quality Standard

本仓库把 Skill 当成可维护产品，而不是一段一次性提示词。每个发布版应同时满足以下特性。

| 特性 | 发布要求 | 仓库门禁 |
| --- | --- | --- |
| 可发现 | `name` 与目录一致；描述明确说明能力、典型触发和相邻不适用场景 | `check_all_skills.py` + `trigger-evals.json` |
| 可执行 | 一个清晰的主工作流；确定性、重复性工作优先交给随 Skill 提供的脚本 | 人工评审 + 行为 eval |
| 有边界 | 明确授权、隐私、外部副作用和失败时的停止/降级规则 | 行为 eval + 单元测试 |
| 可信 | 区分证据、推断和未知；不把缺失数据伪装成结论 | 行为 eval + 领域测试 |
| 可验证 | 产物、状态、错误和下一步可检查；脚本输出稳定的机器可读结果 | schema + 单元测试 |
| 轻量 | `SKILL.md` 保持在 500 行以内，细节按需放入 `references/` | 结构检查 |
| 可维护 | 面向人类的 README、可用示例、版本化测试和真实近邻触发样本 | 结构检查 + CI |
| 高性能 | 无网络的诊断与路由不应成为交互瓶颈 | `benchmark_skill_entrypoints.py` |

## 发布检查

每次新增或修改 Skill，先运行：

```bash
python3 scripts/check_all_skills.py
python3 scripts/benchmark_skill_entrypoints.py
python3 -m unittest discover -s tests -p 'test_*.py'
```

再检查对应 `evals/<skill_name>/`：行为集至少 10 个案例，每例至少 3 个可判断的预期；触发集至少 20 个贴近真实的近邻请求，并且正、负例各至少 8 个且数量相等。新增能力、授权边界、降级路径或触发词时，必须先更新相应案例。

## 人工发布评审

自动化不替代判断。发布前确认：描述不会误抢相邻任务；工作流没有不必要的确认或上下文加载；失败时没有暗中猜测或扩大权限；示例不含真实私密数据、密钥或生成物；对用户的最终说明清楚交代已完成、未完成和下一步。
